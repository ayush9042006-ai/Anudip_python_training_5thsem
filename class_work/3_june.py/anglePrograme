angle1 = float(input("Enter length: "))
if angle1 <= 0:
    print("traingle cannot be negative.")
    exit()

angle2 = int(input("Enter width: "))
if angle2 <= 0:
    print("triangle cannot be negative.")
    exit()
angle3 = int(input("Enter length: "))
if angle3 <= 0:
    print("triangle cannot be formed")
    exit()

if ((angle1+angle2+angle3)==180):
    
    if(angle1 < 90 and angle2 < 90 and angle3 < 90):
        print("triangle is acute angle.....")
    elif(angle1 == 90 or angle2 == 90 or angle3 == 90):
        print("triangle is right angle.....")
    else:
        print("triangle is obtuse.....")


else:
    print("triangle is not formed.....")


